import { memo } from "preact/compat";

const First = () => {
  return (
    <>
      <h2 className="text-2xl font-bold mb-4">DB Modeling </h2>
      <div className="flex flex-col gap-3">WIP</div>
    </>
  );
};

export default memo(First);
